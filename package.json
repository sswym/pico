{
  "name": "srcode",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Vibe coding agent with long-term memory",
  "type": "module",
  "private": true,
  "bin": {
    "srcode": "./bin/srcode.ts"
  },
  "scripts": {
    "build": "bun run scripts/build.ts",
    "start": "bun run bin/srcode.ts",
    "test": "bun test",
    "verify": "bunx tsc --noEmit && bun test",
    "update-deps": "bash scripts/update-deps.sh"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.82.1",
    "@earendil-works/pi-ai": "^0.82.1",
    "@earendil-works/pi-coding-agent": "^0.82.1",
    "@earendil-works/pi-tui": "^0.82.1",
    "ignore": "^7.0.5",
    "node-html-parser": "^9.0.0"
  }
}
